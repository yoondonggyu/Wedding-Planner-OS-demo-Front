# 노션 API 명세서 템플릿

## 템플릿 구조

### 기본 구조

```
## {API 번호} {API 이름}

**설명:** {API 설명}

---

### 📋 요청 구조

#### Request Header

| 헤더 | 타입 | 필수 | 설명 |
|------|------|------|------|
| Content-Type | String | 필수 | application/json |
| Authorization | String | 필수/선택 | Bearer {token} |

#### Request Body

```json
{
  "field1": "string",
  "field2": "number"
}
```

**필수 필드:**
- `field1`: string - 설명
- `field2`: number - 설명

**선택 필드:**
- `field3`: string | null - 설명

#### Query Parameters

| 파라미터 | 타입 | 필수 | 설명 |
|----------|------|------|------|
| page | Integer | 선택 | 페이지 번호 |
| limit | Integer | 선택 | 페이지당 개수 |

#### Path Parameters

| 파라미터 | 타입 | 필수 | 설명 |
|----------|------|------|------|
| id | Integer | 필수 | 리소스 ID |

---

### 📤 응답 코드별 예시

| Status Code | Body | Message |
|-------------|------|---------|
| 200 | `{...}` | 성공 메시지 |
| 400 | `{...}` | 에러 메시지 |
| 401 | `{...}` | 인증 실패 |
| 404 | `{...}` | 리소스 없음 |
| 422 | `{...}` | 검증 실패 |
| 500 | `{...}` | 서버 오류 |

---

## 실제 예시: 2.1 프로필 이미지 업로드 (multipart/form-data)

## 2.1 프로필 이미지 업로드

**설명:** Multipart 이미지 업로드, 응답으로 CDN URL 반환

---

### 📋 요청 구조

#### Request Header

| 헤더 | 타입 | 필수 | 설명 |
|------|------|------|------|
| Content-Type | String | 필수 | multipart/form-data |
| Authorization | String | 필수 | Bearer {token} - JWT 액세스 토큰 |

#### Request Body (multipart/form-data)

| 필드명 | 타입 | 필수 | 설명 |
|--------|------|------|------|
| file | File | 필수 | 업로드할 이미지 파일 (jpg, png, jpeg만 가능, 최대 5MB) |

> 💡 **multipart/form-data 형식:**
> - Content-Type 헤더는 브라우저가 자동으로 설정합니다 (boundary 포함)
> - 파일 필드는 `file` 이름으로 전송합니다
> - 지원 형식: jpg, png, jpeg
> - 최대 크기: 5MB

**필수 필드:**
- `file`: File (필수) - 업로드할 이미지 파일 (jpg, png, jpeg만 가능, 최대 5MB)

#### Query Parameters

없음

#### Path Parameters

없음

---

### 📤 응답 코드별 예시

| Status Code | Body | Message |
|-------------|------|---------|
| 200 | ```json<br>{<br>  "message": "upload_success",<br>  "data": {<br>    "image_url": "https://cdn.example.com/image.jpg"<br>  }<br>}``` | 이미지 업로드 성공 |
| 400 | ```json<br>{<br>  "message": "file_required",<br>  "error_code": 4003,<br>  "data": null<br>}``` | 파일을 선택해주세요 |
| 400 | ```json<br>{<br>  "message": "invalid_file_type",<br>  "error_code": 4004,<br>  "data": {"allowed": ["jpg", "png", "jpeg"]}<br>}``` | 지원하지 않는 파일 형식입니다 |
| 413 | ```json<br>{<br>  "message": "file_too_large",<br>  "error_code": 4131,<br>  "data": {"max_size": "5MB"}<br>}``` | 파일 크기가 5MB를 초과합니다 |

---

## 실제 예시: 1.1 로그인

## 1.1 로그인

**설명:** 이메일/비밀번호 검증 후 JWT 토큰 발급

---

### 📋 요청 구조

#### Request Header

| 헤더 | 타입 | 필수 | 설명 |
|------|------|------|------|
| Content-Type | String | 필수 | application/json |

#### Request Body

```json
{
  "email": "string",
  "password": "string"
}
```

**필수 필드:**
- `email`: string - 사용자 이메일 주소
- `password`: string - 사용자 비밀번호

#### Query Parameters

없음

#### Path Parameters

없음

---

### 📤 응답 코드별 예시

| Status Code | Body | Message |
|-------------|------|---------|
| 200 | ```json<br>{<br>  "message": "login_success",<br>  "data": {<br>    "access_token": "eyJ...",<br>    "token_type": "bearer",<br>    "user_id": 1,<br>    "nickname": "사용자",<br>    "profile_image_url": "https://..."<br>  }<br>}``` | 로그인 성공 |
| 400 | ```json<br>{<br>  "message": "invalid_credentials",<br>  "error_code": 4001,<br>  "data": null<br>}``` | 아이디 또는 비밀번호를 확인해주세요 |
| 422 | ```json<br>{<br>  "message": "email_required",<br>  "error_code": 4221,<br>  "data": null<br>}``` | 이메일을 입력해주세요 |
| 422 | ```json<br>{<br>  "message": "password_required",<br>  "error_code": 4224,<br>  "data": null<br>}``` | 비밀번호를 입력해주세요 |

---

## 노션 사용 팁

### 1. Callout 블록 활용
- 💡 **설명**: API 설명을 Callout으로 강조
- ⚠️ **주의**: 에러 케이스나 주의사항
- ✅ **성공**: 성공 응답 예시
- ❌ **에러**: 에러 응답 예시

### 2. Code 블록
- JSON 형식은 Code 블록으로 작성
- 언어를 'JSON'으로 설정
- 여러 줄 JSON은 들여쓰기 유지

### 3. Toggle 블록
- Request/Response 예시를 Toggle로 접기/펼치기
- 여러 에러 케이스를 Toggle로 정리
- 예시:
  ```
  > 📤 200 응답 예시
  > {JSON 내용}
  ```

### 4. Database (Table)
- API 목록을 Database로 관리
- 컬럼: API 번호, 이름, HTTP 메서드, Path, 인증 여부
- 필터링, 정렬 기능 활용
- 각 행을 클릭하면 상세 페이지로 이동

### 5. Template Button
- 새 API 추가 시 템플릿 자동 생성
- 위의 기본 구조를 Template로 저장

### 6. 페이지 링크
- API 목록 Database에서 각 API 상세 페이지로 링크
- 사이드바에 API 카테고리별로 페이지 구성

### 7. 색상 코딩
- Status Code별로 색상 구분
  - 200: 초록색
  - 400: 주황색
  - 401: 노란색
  - 404: 빨간색
  - 422: 주황색
  - 500: 빨간색

---

## 노션 템플릿 복사 방법

1. 위의 마크다운 내용을 복사
2. 노션에서 `/markdown` 입력
3. 붙여넣기
4. 각 섹션을 노션 블록으로 변환
5. Callout, Toggle, Code 블록 등으로 스타일링

